---
const { name, icon, type } = Astro.props;
---

<button class="bg-rose-500 rounded-lg flex items-center py-2 px-3 h-11 text-white font-medium">
  {name}
  <img class="ml-3" src={`/${icon}.svg`} alt={icon}>
</button>